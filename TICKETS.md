# Ticket Tracking Moved

Tickets now live as individual files under `tickets/`.

Use filenames to discover status: open tickets omit `_complete`; completed tickets end in `_complete.md`. See `tickets/README.md` for the repository-wide ticket rules.
